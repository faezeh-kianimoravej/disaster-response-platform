spring:
  config:
    activate:
      on-profile: local-docker
  cloud:
    discovery:
      enabled: true
    gateway:
      discovery:
        locator:
          enabled: true
      # You can add docker-specific route overrides here if needed
      # routes: []
eureka:
  client:
    enabled: true
    service-url:
      defaultZone: http://discovery-service:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
